//import 'package:flutter/material.dart';
//import 'package:revision/qoutes.dart';

/*class Quetetemplate extends StatelessWidget {
  Qoutes q;
  Function delete;

  Quetetemplate({this.q, this.delete});

  @override
  Widget build(BuildContext context) {
    return Card(
      margin: EdgeInsets.fromLTRB(16, 16, 16, 0),
      child: Column(
        children: <Widget>[
          Text(
            q.autor,
            style: TextStyle(fontSize: 25),
          ),
          SizedBox(
            height: 6.0,
          ),
          Text(
            q.qoute,
          ),
          FlatButton.icon(
              onPressed: () {
                delete();
              },
              icon: Icon(Icons.delete),
              label: Text("delete this qoute"))
        ],
      ),
    );
  }
}*/
